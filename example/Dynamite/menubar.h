#pragma once

#include <imgui.h>
#include <vector>
#include <string>

using namespace std;

class MenuBar {
public:
    void show();
    //void createMenu(vector<std::string> &menuItems);
    //void createMenu(vector<std::string> menuItems);
};